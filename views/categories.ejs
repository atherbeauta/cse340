<%- include('partials/header') %>

<main>
    <h2>Project Categories</h2>
    <ul>
        <% categories.forEach(category => { %>
            <li><%= category %></li>
        <% }); %>
    </ul>
</main>

<%- include('partials/footer') %>