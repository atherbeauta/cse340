W01 Coaching Session: Server-side Programming
Student: Kazadi Athanase Muamba
AI Coach: Learning Coach
Date: 2026-05-21

--- Full Coaching Session Transcript ---

AI: Welcome! Just to be clear from the start, this is a coaching session, not a test. It is perfectly okay—and even encouraged—to say 'I don't know' or 'I made a mistake.' That's the fastest way we'll learn today. I am your supportive Learning Coach and Software Mentor. May I ask how you would like to begin?

Student: I want to make sure we cover the required topics for the W01 assignment and include the code snippets from my project.
AI: Great, let’s start with your Express routing in `server.js`. Can you tell me how your route handlers are organized and what each one does?

Student: I have a home route, a categories route, and a new organizations route.
AI: Excellent. Here is the actual Express routing code from `server.js`:

```js
// Provide the current year to all templates dynamically
app.use((req, res, next) => {
    res.locals.year = new Date().getFullYear();
    next();
});

// Home route
app.get('/', (req, res) => {
    res.render('index', { title: 'Home' });
});

// Categories route
app.get('/categories', (req, res) => {
    const categories = ['Environment', 'Educational', 'Community Service', 'Health & Wellness'];
    res.render('categories', { title: 'Categories', categories });
});

// Organizations route
app.get('/organizations', (req, res) => {
    const organizations = [
        {
            name: 'Pathway Promise',
            image: '/images/org-1.svg',
            description: 'Supporting students with mentorship, scholarships, and community outreach.'
        },
        {
            name: 'Campus Care Collective',
            image: '/images/org-2.svg',
            description: 'Delivering wellness programs, volunteer events, and neighborhood improvements.'
        },
        {
            name: 'Innovation Impact',
            image: '/images/org-3.svg',
            description: 'Connecting learners with service, sustainability, and technology projects.'
        }
    ];
    res.render('organizations', { title: 'Organizations', organizations });
});
```

AI: This shows how Express routing works: `app.get()` registers a handler for a path, and `res.render()` sends an EJS page to the browser. What is the benefit of using `res.locals.year` in middleware instead of passing `year` in every route manually?

Student: It means I only set the year once and every view can use it without repeating the code.
AI: Exactly. That keeps the routing code cleaner and avoids duplication.

--- JavaScript Language Principles ---
AI: Let’s move to JavaScript principles. I want to discuss a function snippet showing modern JS concepts. Do you see any examples of `const`, `let`, or async functions in your project?

Student: Yes, I use `const` and an async-style snippet in the discussion file.
AI: Here is a relevant JavaScript snippet that demonstrates scoping, error handling, and async code:

```js
async function fetchOrganizations(fetchFn) {
  try {
    const res = await fetchFn('/api/organizations');
    const data = await res.json();
    return data;
  } catch (err) {
    console.error('Fetch failed', err);
    throw err;
  }
}

fetchOrganizations(window.fetch).then(orgs => {
  console.log('Loaded', orgs.length, 'organizations');
}).catch(() => {
  console.error('Could not load organizations');
});
```

AI: This snippet illustrates `const` for stable references, `async/await` for promise handling, and a `try/catch` block for errors. How does using `async/await` improve readability compared to raw `.then()` chaining?

Student: It makes the code look more like synchronous code and easier to follow.
AI: Right — that is one of the main benefits.

--- EJS Partials and Pages ---
AI: Next, we’ll cover EJS partials and pages. Your views use shared partials to avoid repeating layout code. Can you explain how the header and footer are reused across pages?

Student: I include them in each page with EJS includes.
AI: Correct. Here is the EJS page snippet that uses partials in `views/organizations.ejs`:

```ejs
<%- include('partials/header') %>

<main class="page-content organizations-page">
  <section class="section-header">
    <h2>Community Organizations</h2>
    <p>Meet the organizations in our community.</p>
  </section>

  <ul class="organization-grid">
    <% organizations.forEach(function(org) { %>
      <li class="organization-card">
        <img src="<%= org.image %>" alt="Logo for <%= org.name %>">
        <h3><%= org.name %></h3>
        <p><%= org.description %></p>
      </li>
    <% }); %>
  </ul>
</main>

<%- include('partials/footer') %>
```

AI: This shows how each page can render dynamic content while reusing the same header and footer HTML. Why is using partials beneficial for a project like yours?

Student: Because it keeps the layout consistent and makes maintenance easier.
AI: Exactly — one change to a partial updates every page.

--- Frontend CSS ---
AI: Finally, let’s discuss frontend CSS. I’ll show a snippet from `public/css/styles.css` and ask how it affects the design. Ready?

Student: Yes.
AI: Here is the CSS for the home image and footer styling:

```css
.hero-media img {
  width: min(100%, 560px);
  border-radius: 1rem;
  border: 1px solid rgba(64, 96, 255, 0.12);
  box-shadow: 0 24px 50px rgba(32, 51, 79, 0.08);
}

.site-footer {
  margin-top: auto;
  padding: 1.25rem 2rem;
  text-align: center;
  color: var(--text-muted);
  background: rgba(255, 255, 255, 0.92);
  border-top: 1px solid var(--border);
}
```

AI: This CSS helps make the hero image responsive and gives the footer a clean, readable layout. What visual effect does the `box-shadow` add to the image?

Student: It makes the image feel raised and adds depth.
AI: That’s right — it improves the page’s visual hierarchy.

--- Session Wrap-up ---
AI: We have now covered all four required topics: Express routing, JavaScript language principles, EJS partials and pages, and frontend CSS. I also included four code snippets and asked guiding questions about each one. Is there anything else you want to review before we finish?

Student: No, this looks complete. Thank you.
AI: You’re welcome. I’m glad we could cover the session fully.

--- End of Transcript ---
